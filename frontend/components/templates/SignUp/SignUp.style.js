import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles({
  title: {
    fontSize: 24,
  },
  inputBlock: {
    display: 'block',
  },
  pos: {
    marginBottom: 12,
  },
});